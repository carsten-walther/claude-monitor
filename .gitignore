# WiFi, API and OTA credentials - never commit
secrets.yaml
daemon/secret.txt

# ESPHome build tree, downloaded fonts and icons, compiled firmware
.esphome/
*.bin

# macOS
.DS_Store

# Python Cache
__pycache__/

# py2app build artifacts (daemon/menubar_app.py -> daemon/setup.py)
daemon/.eggs/
daemon/build/
daemon/dist/
